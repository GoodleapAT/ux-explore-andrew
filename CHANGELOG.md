# Changelog

**What changed, when, and who else needs to know. This is the only file that answers "has anything
changed since I last looked".**

> **Who may write to this file:** any surface that can write, and it **must**, every time it changes
> anything in this repository. **Append only.** Newest at the bottom. Never edit or delete a row.
>
> **Every surface reads this file at the start of a session and says out loud the date of the newest
> entry.** That one sentence is the whole sync mechanism. If a Claude Design session says 9 September
> and a Cowork session says 11 September, Design is stale and needs re-pointing.
>
> **Surfaces that cannot write** must include a row for this file in their write-back block.
>
> One row per meaningful change. A typo fix is not a meaningful change. A changed rule, a new
> decision, a moved file, a source that turned out to be wrong, or a new document all are.

## The columns

| Column | What goes in it |
|---|---|
| **Date** | The date of the change |
| **What changed** | One sentence, in plain language. Enough that somebody can tell whether it affects them |
| **Files** | Which files. Name them, because that is how a reader decides whether to re-read |
| **From** | Which surface made the change: Cowork, Claude Code, Claude chat, Claude Design via a write-back |
| **Who else needs to know** | Which other surfaces are now stale, and what to do. `None` is a valid answer and usually the right one |

---

| Date | What changed | Files | From | Who else needs to know |
|---|---|---|---|---|
| 2026-09-11 | Working memory created: start-here protocol, global sources, idea inbox, process decision log, five standards | Root files and `standards/` | Cowork | None, first version |
| 2026-09-11 | Restructured to multi-project: global at the root, project folders below. Vocabulary moved into the project, idea catalogue moved with it | `START-HERE.md`, `upgrade-to-pros/` | Cowork | None, nothing was pointing at it yet |
| 2026-09-11 | Repository hygiene standard added, after an organisation ruleset blocked commits to main. A gitignore was added | `standards/repository-hygiene.md`, `.gitignore` | Claude Code | None |
| 2026-09-11 | Repository moved from `loanpal-engineering/UX-Explore` to `GoodleapAT/ux-explore-andrew`, which also absorbed the hosted prototypes | Everything, plus the read-me-first file outside the repository | Cowork | Any surface pointed at the old repository, which is all of them. The old one can be deleted |
| 2026-09-11 | Claude Design context written: where the model landed, the five differences from DRAFT v3, all eighteen scenario rows, the UI history, and the read-only write-back protocol | `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md` | Cowork | Claude Design. Point it at the repository and have it read this document first |
| 2026-09-11 | Scenario pages: recorded that the Entities column is generated rather than authored, so the scenarios are authoritative on the experience and not on the model. Review status is a marker on the page | `upgrade-to-pros/SOURCES.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md` | Cowork | Any surface that has already read a scenario and treated the Entities column as a specification |
| 2026-09-11 | This changelog created, with prompting triggers and a per-surface staleness table added to the protocol | `CHANGELOG.md`, `START-HERE.md`, both read-me-first files | Cowork | All surfaces, at the start of their next session |
| 2026-09-11 | Claude Design re-pointed at the moved repository and told what changed. First live use of the sync mechanism | None, an action rather than a change | Cowork | None |
| 2026-09-11 | Joel's ux-factory recorded: what it is, what is worth borrowing, what does not transfer, the board's nav contract and the two read-only boundaries. Three new idea records and a need | `upgrade-to-pros/SOURCES.md`, `upgrade-to-pros/IDEAS-CATALOGUE.md`, `IDEAS-INBOX.md` | Cowork | Claude Design, if it has been asked about page structure. The board has a UX DRAFT section with nine archetypes and a nav contract that the context document does not yet mention |
| 2026-09-11 | **Token correction.** Joel's team reports the Merlin file is a mirror of Sol carrying only the 113 semantic tokens, with no radius, spacing or font scale. Our extracted stylesheet is that mirror | `SOURCES.md` | Cowork | Claude Design and any surface building mockups. The token file is incomplete, not wrong. Re-extract from Sol before the next round |
| 2026-09-11 | The nav contract, the parallel UX effort and the token caveat added to the Claude Design context, so Design reads them from the repository rather than from a message. Working voice gained a rule: writing a changelog row is not the same as telling Andrew | `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md`, `standards/working-voice.md` | Cowork | **Claude Design.** Have it re-read the context document, which now carries the nav contract and the token caveat |
| 2026-09-11 | **Reframed Joel's UX factory as external reference rather than an input.** New standard on handling other people's parallel work: reference never input, say whose it is, nothing crosses over until Andrew says so, do not manufacture urgency from it. The nav contract is recorded as their position and explicitly not binding. N-004 recast as M-005, a move Andrew may take rather than a gap he must fill | `standards/external-work.md`, `upgrade-to-pros/SOURCES.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md`, `upgrade-to-pros/IDEAS-CATALOGUE.md`, `IDEAS-INBOX.md` | Cowork | **Claude Design.** Re-read the context document. Its section on the other effort now says plainly that none of it is a requirement |
| 2026-09-11 | Mockup density gained a rule: **no reference codes or record identifiers**. Call the thing by its name. They cost row space, buy nothing on a structural question, and make a screen look drawn against a system of record that exists | `standards/mockup-density.md` | Cowork | **Claude Design.** Applies to anything already built. The five views in memo 6 still carry codes and are now out of step with the standard |
| 2026-09-11 | **Component library standard added.** Atomic design principles, six fields recorded at creation, a declared position per product tier for when a component's subject is absent, one component with states rather than near-identical siblings, and the library repository-resident rather than living in CD. Surface short forms defined: C, CC, CD | `standards/component-library.md`, `START-HERE.md`, `DECISIONS.md` | C | **CD.** It changes how every component is recorded from now on, and it should know the destination before it builds more screens |
| 2026-09-11 | **Worked example written**, carrying the complete Thompson mock data: people, contacts, property, prior project and service plan, three leads with the customer's own words, proposal and versions, both scope itemisations, the full dated sequence, task lists, sub-status registers and the text of every note and history entry. Shared spine plus two tellings, cash and financed. **Eight refinements to S15 recorded and attributed to C**, including the final-bill arithmetic the scenario page flags itself | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/OVERVIEW.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md` | C | **CD.** Read the worked example before building anything for S15. It replaces inventing mock data, which is what would otherwise have happened across nine columns |
| 2026-09-13 | **Three project registers added.** Objects the model does not have, with criticality, intent and hold. Jobs to be done, ten of them, with a slot for cross-referencing solutions. Deviations, recording where a mock departs from a scenario and the date of the scenario version it was built against | `upgrade-to-pros/OBJECT-REGISTER.md`, `upgrade-to-pros/JOBS-TO-BE-DONE.md`, `upgrade-to-pros/DEVIATIONS.md` | C | **CD.** Every mock now records the scenario version it was built against, and anything the model lacks goes in the register rather than being invented or refused |
| 2026-09-13 | **Standards rewritten.** Every rule marked Always, Default or Prefer. Worked examples added. **The badge ban is corrected to limited use**, with a test and a fallback ladder for states that cannot use colour. The density and component-library contradiction resolved: a capability that will exist advertises, a gap the model cannot answer is left out. Stage flex added alongside tier flex | All of `standards/`, plus the new `standards/INDEX.md` | C | **CD.** The badge correction changes screens already built. The index is the file to read when unsure which rule applies |
| 2026-09-13 | **Corrections from the S15 review.** Treatments B and C stand and A is retired. The customer record regroups without nesting. Job costs corrected to CD's split. Memo 6 superseded as the current UI position. Fourteen component records moved from CD into the repository | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/components/ITERATION-2.md`, `DECISIONS.md` | C | **CD.** Memo 6 is no longer the reference. The component records are now repository-resident, and two of the standards changed under them |
| 2026-09-13 | **Drift tracking built.** Our model serialised with a read date and diffed against DRAFT v3. A drift check register listing every source somebody else can change, with the date we last looked and a ten-minute routine. A checked-on log added to the deviations register. **First run found our own board diagram five decisions behind our own decision log**, and found the team's dated board snapshot and 47-name canonical entity list, which supersede reading their board by eye | `upgrade-to-pros/MODEL-SNAPSHOT.md`, `upgrade-to-pros/DRIFT-CHECK.md`, `upgrade-to-pros/DEVIATIONS.md`, `upgrade-to-pros/OVERVIEW.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md` | C | **CD.** Not in the model now has an anchor and a date. Also worth knowing: Service Plan is parked dark in all seventeen team scenarios as a placeholder, not a classification |
| 2026-09-13 | **Correction to the model snapshot.** It claimed the board was silent on when the project is created. It is not: the board says so in two places, on the Project card and on the connector from Opportunity. Four deltas between board and decision log, not five | `upgrade-to-pros/MODEL-SNAPSHOT.md` | C | None. Nothing was built on the wrong claim |
| 2026-09-13 | **Comprehensive model comparison written.** Thirty eight differences grouped by theme, each with a proposed classification and a ruling column. First comparison using their full edge list rather than their entity names, which changed several readings. **Four genuine conflicts, twenty one omissions on our side, two ambiguities to take to the team, and six things that turn out not to be differences at all** | `upgrade-to-pros/MODEL-COMPARISON.md`, `upgrade-to-pros/OVERVIEW.md` | C | **CD.** The context document says the invoice at the project is a live disagreement. It is not. Their edge is solid and the two models agree |
| 2026-09-13 | **Review artifact built for the model comparison.** Thirty seven rows, each pre-filled with the proposed reading so only disagreements need touching, plus per-row comments, filters, a bulk accept per section, browser-local saving and a markdown export that reports untouched rows as still proposed rather than settled | `upgrade-to-pros/MODEL-COMPARISON-REVIEW.html` | C | None until Andrew has worked through it. His export comes back here and becomes rulings in the comparison file |
| 2026-09-14 | **Confused added as a ruling option**, distinct from Undecided: Undecided is about the world, Confused is about my explanation. Flagged rows are excluded from the ruled count and listed first in the export. Andrew's first three comments recorded, and **the model snapshot's claim that the project is "mandatory, always" corrected**, which he challenged and was right about | `upgrade-to-pros/MODEL-COMPARISON-REVIEW.html`, `upgrade-to-pros/MODEL-COMPARISON.md`, `upgrade-to-pros/MODEL-SNAPSHOT.md` | C | **CD.** Ours no longer claims a project exists for every lead. A lead nobody pursues has no project |
| 2026-09-14 | **Principles section added to the project decision log**, above the dated rows. Two: money is stored where it occurs and higher levels derive, which explains five things including why per-job margin is unavailable and where the subscription invoice defect comes from; and a project is one workspace that transforms rather than a sequence of screens, of which the stage-flex decision is an instance | `upgrade-to-pros/DECISIONS.md` | C | **CD.** Per-job margin being absent is now a consequence of a rule rather than a gap, and should be described that way if it comes up |
| 2026-09-14 | **Beats drafted for a lead becoming a project.** Seven beats from arrival to lost or sold, three variants for what triggers the container, naming candidates for the lead states, and the three separate problems of advertising, triggering and showing a transition. **The trigger may be assignment or claim rather than a button**, since both already carry the intent to sell | `upgrade-to-pros/BEATS-lead-to-project.md`, `upgrade-to-pros/MODEL-COMPARISON.md` | C | **CD.** D-01 is a position rather than undecided. The project need not be strongly represented on day one, and a project marked lost leaves its material with the leads |
| 2026-09-14 | **Five beats settled.** Sales sees unvalidated leads behind a default filter rather than a permission. Assignment or claim creates the project, with no separate act needed. The container is **named by its contents**, so the name grows when a lead is attached. The project **escalates through three forms**, implied, then a side pane, then a page. And **the word Project need never appear in the interface**, which removes the Merlin naming collision from the product while leaving it in the model | `upgrade-to-pros/BEATS-lead-to-project.md` | C | **CD.** The container may have no page of its own at early stages, and screens should not assume one |
| 2026-09-14 | **In-depth folder created**, for documents that answer one question and then stop changing, with the rule that a concluded document's settlements go to the decision log while the document keeps the reasoning. Beats document moved in. **Decision log gained a Standing column**: Working, Raised, Agreed, so a direction Andrew is exploring cannot be quoted as something the group settled. Fifteen existing rows backfilled. **Six beat settlements logged** | `upgrade-to-pros/in-depth/`, `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/OVERVIEW.md` | C | **CD.** A Working decision binds design work exactly as an Agreed one does. The difference is only how hard it is to reverse |
| 2026-09-14 | **Glossary added at the root**, holding all eighteen controlled vocabularies in one place with their values defined: standing, rigidity, surface, access, idea kind and status, appetite, support, criticality, intent, hold, object status, deviation kind and resolution, comparison classification, document status, absence position, stage position, product tier. **Four live word collisions named**, three with a proposed rename: Raised means two things, Agreed collides with Agreement, and the deviation kind Proposal collides with the entity. A new rule: a vocabulary goes in the glossary in the session that invents it. **Atomic level is required on every component record and has never been enumerated**, which the glossary records as a gap. The domain status sets, eight of them, moved out of the statuses memo into the project vocabulary file where they belong | `GLOSSARY.md`, `upgrade-to-pros/VOCABULARY.md`, `START-HERE.md`, `standards/INDEX.md` | C | **CD.** The status sets are now in the vocabulary file rather than only in a memo, and they are a working set rather than ratified. **Three rename proposals are pending Andrew's ruling**, so do not use the new words yet |
| 2026-09-14 | **The structure has a name: the Product Thinking System.** Chosen so it survives being said by somebody who has not read any of it. Also logged: the glossary rule, that a controlled vocabulary is defined at the root in the session that invents it | `DECISIONS.md` | C | **CD and the Claude chat project.** Both carry stored instructions that describe this structure without naming it. Worth adding the name when either is next refreshed |
| 2026-09-14 | **Joel's UX factory declined as a framework, and its goal adopted as our own.** The root decision log gains a principles section, global ones numbered G, and its scope widens to cover where the work is heading when the destination changes something today. **G-1: the component library is the engine, not the residue.** Component records gain a seventh required field, what variation the component absorbs and what would instead mean a new component, which resolves the tension between building the library and building fast: what makes a component brittle is a record that presents it as finished, not the cost of writing it down. **Four of the five per-view elements Andrew wants already exist in this system**, rendered per view rather than newly built; only upsell opportunities is new | `DECISIONS.md`, `standards/component-library.md`, `standards/INDEX.md`, `upgrade-to-pros/IDEAS-CATALOGUE.md`, `upgrade-to-pros/SOURCES.md`, `IDEAS-INBOX.md` | C | **CD, and anything that has written a component record.** Seven fields now, not six. The fourteen existing records are incomplete rather than wrong and gain the field when next touched. Also: the generative ambition **justifies nothing on its own** until a scenario has actually been composed from the library |
| 2026-09-14 | **The first three rulings seeded into the review form**, so they no longer have to be re-entered. Seeded only where the browser has nothing saved for that row, so anything already entered there always wins. **D-03 reclassified from Undecided to Ours simplifies**, since P-1 gives the rule but not the machinery and theirs names an entity that holds the roll-up. D-02 stays a conflict so it travels with D-01, and is explicitly not a fifth conflict | `upgrade-to-pros/MODEL-COMPARISON-REVIEW.html`, `upgrade-to-pros/MODEL-COMPARISON.md` | C | None. The count of four conflicts is unchanged |
| 2026-09-14 | **Rulings through D-08 written into the review form itself**, after Andrew lost a pass to a restart. The form is no longer the only copy: it now seeds from the file and the browser's copy always wins. **D-04, D-05 and D-07 all moved to Undecided**, against my readings of ours-simplifies, conflict and ours-simplifies. That drops the conflict count from four to three, since lead granularity is no longer one. **D-04 opens a question underneath the one asked**: customer role against property, whether they own it or merely live there. D-06 carries a comment and no ruling, on purpose | `upgrade-to-pros/MODEL-COMPARISON-REVIEW.html`, `upgrade-to-pros/MODEL-COMPARISON.md` | C | **CD.** Lead granularity is a preference rather than a settled position, and the property anchor is now open rather than adopted from theirs |
| 2026-09-14 | **The comparison is ruled, thirty one of thirty seven, and the headline is that our model concedes.** Twenty two rows are adoption, so **our model stops being a separate model and becomes DRAFT v3 plus three disagreements and six open questions.** New classification **Adopt theirs** added, because Agreement was being used to mean "no argument here" while the comments meant "take theirs", and the two have opposite consequences: one closes a row, the other puts an entity into the model. The entities our model gains are now listed in one place. **Conflicts down to three**, and the subscription invoice defect is contingent on the service question rather than conceded | `upgrade-to-pros/MODEL-COMPARISON.md`, `upgrade-to-pros/MODEL-COMPARISON-REVIEW.html`, `GLOSSARY.md` | C | **CD, and this is the big one.** Anything describing ours as a rival model is now wrong. Read the adoption list before drawing anything in selling, money or services, because most of those rows gained entities. The financing spine in particular is no longer a gap |
| 2026-09-14 | **Three objects registered out of the ruling comments.** Forms and checklists, which we keep drawing while declining to adopt their entity, the only such case. **Customer role against a property**, blocking, which sat underneath the anchoring question and would have been closed silently by adopting their dual anchor. And how site detail references the property | `upgrade-to-pros/OBJECT-REGISTER.md`, `IDEAS-INBOX.md` | C | **CD.** Customer role against property is unresolved in both models, so a screen must not imply the customer owns the address |
| 2026-09-14 | **The comparison is closed. All thirty seven rows ruled.** The last six: a lead surviving decline keeps ours, site assessment became its own thread rather than a classification, **one scope equals one job** settles our side and leaves the ambiguity as theirs, forms and checklists and documents and photos are **four elements stored by the job with the list explicitly not exhaustive**, and **the service agreement question is deferred**, which keeps the subscription invoice defect contingent rather than settled. Five decisions logged | `upgrade-to-pros/MODEL-COMPARISON.md`, `upgrade-to-pros/MODEL-COMPARISON-REVIEW.html`, `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/OBJECT-REGISTER.md`, `upgrade-to-pros/in-depth/site-assessment.md` | C | **CD.** The job stores four named things and the list is open, so do not treat it as complete. Service is not settled either way, so a mock should say which reading it assumes |
| 2026-09-14 | **The team settled lead granularity and the mint point, both against us, hours after the comparison closed.** A lead is an **engagement carrying several interests**, because a CSR discussing several trades should not work across several views. The project is created **when a proposal or proposals are won**, which is a third position, later than ours and later than DRAFT v3's. Five decisions logged, two Agreed and three consequences. **The beats document is concluded as superseded on its premise**, with a table of which of its six settlements survive. **The worked example is restated**: the Thompsons now have one engagement with three interests, and the project appears on 18 August at the win rather than on 12 August at the decision to pursue | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/OVERVIEW.md`, `upgrade-to-pros/MODEL-COMPARISON.md`, `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/MODEL-SNAPSHOT.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project.md` | C | **CD, urgently, and everything else.** One lead per trade is dead and so is the project existing during selling. **The lead is now the selling workspace.** Any screen showing a project before a win is wrong, and any screen listing one lead per trade is wrong. The worked example changed, so re-read it before drawing anything |
| 2026-09-14 | **The drift check register had a gap and it cost us.** Every source it watches is an artefact, and this change arrived as a decision taken in conversation, so nothing we were watching moved. A non-artefact row added: the team's current position on anything we hold a contrary position on, checkable only by asking. **Where our model disagrees with theirs, the disagreement is re-confirmed before it is designed against** | `upgrade-to-pros/DRIFT-CHECK.md`, `upgrade-to-pros/MODEL-SNAPSHOT.md` | C | None, but it is the most useful thing learned today |
| 2026-09-14 | **Job J-11 added**, updating several things a customer is talking about in one conversation. **The first job in the register that arrived as an argument against one of our positions rather than out of a scenario**, and it won | `upgrade-to-pros/JOBS-TO-BE-DONE.md` | C | **CD.** The engagement has to be good at this, and a design can satisfy the decision while failing the job |
| 2026-09-14 | **Warning banner added to the Claude Design context document**, naming its three wrong claims: ours as a rival model, one lead per trade, and the project existing during selling. **A full rewrite is still pending** and Andrew has been told | `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md` | C | **CD.** Read the banner before anything else in that document. The reading order it gives is the reliable path until it is rewritten |
| 2026-09-14 | **The team moved a third time and v4 of their model arrived, so the structure changed rather than the documents.** New file `upgrade-to-pros/TEAM-POSITIONS.md` holds every team position once, with a date, a source and a history table, and **a new Always rule says an external position is recorded in one file and pointed at, never restated.** The decision log, comparison, snapshot, overview, worked example and Claude Design context now cite rows instead of repeating them. Reason: the mint point held **four positions in one day** and each move cost a seven-document sweep | `upgrade-to-pros/TEAM-POSITIONS.md`, `standards/external-work.md`, `standards/INDEX.md`, `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/OVERVIEW.md`, `upgrade-to-pros/MODEL-COMPARISON.md`, `upgrade-to-pros/MODEL-SNAPSHOT.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md`, `upgrade-to-pros/DRIFT-CHECK.md` | C | **CD and every surface.** Do not take the team's position from any document other than the positions file, and cite the row rather than paraphrasing it |
| 2026-09-14 | **The project's creation point resolved close to Andrew's original position**: created when intent to sell is expressed, at qualification or on proposal creation. **v4 also adds Qualified Lead as an entity**, which marks the end of validation, the exact thing we had said their model failed to mark. Four things adopted from v4: Qualified Lead, the **satellite record** distinction, uniform job outer states with trade-specific sub-states, and checkpoints as per-org policy rather than structure. **Four of our adopt-theirs rows were reworded**: Estimate, Scope Component, Job Financials and Commission are satellites, not flow entities | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/TEAM-POSITIONS.md`, `upgrade-to-pros/MODEL-COMPARISON.md` | C | **CD.** The satellite distinction generalises Andrew's own ruling on forms and photos, so it applies to eight things rather than four. Jobs have uniform outer states, so a trade does not change a job's lifecycle shape, only what happens inside it |
| 2026-09-14 | **Worked example project moved back to 12 August**, at qualification, having been moved to 18 August earlier the same day. The engagement and its three interests are unchanged. **A fresh beats document written against v4**, with the old one left concluded as history and its whipsaw recorded rather than tidied away. The new one's real problem is that the engagement and the project are **both live from qualification to the close**, which neither earlier version had to handle | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project.md` | C | **CD.** Roofing is the test case: an interest on the engagement, priced as an option, never a scope or a job. If the engagement does not hold its whole story in one piece, the split is wrong |
| 2026-09-14 | **In-depth folder gained a rule: a document overtaken by somebody else's answer is not the same as one concluded by its own reasoning, and should look different.** Keep the original text, add a box, add a line rather than rewriting the box if the premise moves again, name the replacement, and **never restate the position that overtook it.** Index updated with the three current documents | `upgrade-to-pros/in-depth/README.md` | C | None, but it applies to the site assessment document if the team answers that one before we do |
| 2026-09-14 | **Decoder added for Opportunity, which carries two retired meanings rather than one.** In Model C it was the demand object, now Lead. In the A against B comparison it was a per-trade unit of work, which is now an interest on the engagement, a level below a lead. The vocabulary said only "retired in favour of Lead", which would make a reader mistranslate the older diagrams. **New lesson: a retired word needs its meaning recorded, not just its retirement** | `upgrade-to-pros/VOCABULARY.md` | C | Any surface reading the `earlier/` folder. Three opportunities feeding two scopes only parses on the second meaning |
| 2026-09-14 | **Vocabulary file corrected, it was contradicting the decision log.** Lead now reads as an engagement carrying interests, **Interest added as a settled noun** since it was in constant use and never defined, and the Project row stops stating its creation point and cites T-2 instead. Also flagged: **the lead status set was written for one lead per trade** and probably belongs per interest now, which is unresolved | `upgrade-to-pros/VOCABULARY.md` | C | **CD.** Interest is a defined noun now. Do not put a lead-level status on a lead that holds three interests without saying which grain you mean |
| 2026-09-14 | **Statuses split across two levels.** The interest takes what used to be the lead's set; the lead gets its own, mostly derived. **Qualifying and Disqualified moved up to the lead**, since qualification is an act on the engagement and it is what T-2 hangs the project off. The lead's derived values, active and dormant, mirror the project's shape, which was drawn separately months earlier. **Andrew's framing: a stake in the sand, not a settlement**, because discussion across demand and selling is still running | `upgrade-to-pros/VOCABULARY.md`, `upgrade-to-pros/DECISIONS.md` | C | **CD.** Two status sets now, not one. A demand screen has to say which level it is showing, and a lead's status is not something a user sets |
| 2026-09-14 | **The demand chain changed again and this time the sweep was three rows.** Positions updated rather than documents rewritten: **T-2 is now a lead creating a project**, one to one, and **T-3 is Inquiry or Prospect becoming a Lead when qualified**, so the separate Qualified Lead entity is collapsed and the Lead is itself the qualified thing. **The mint point held five positions in one day**, and the last two are the same moment described differently. Two open questions logged against the rows: whether Inquiry and Prospect are one object, since one is an event and the other a party, and where several trades live | `upgrade-to-pros/TEAM-POSITIONS.md` | C | **CD.** Take the demand chain only from the positions file. Inquiry and Prospect are new nouns and Qualified Lead no longer exists |
| 2026-09-14 | **Status levels corrected down one.** Qualifying and Disqualified were moved up to the lead earlier today and now move to the Inquiry and Prospect, because qualification happens before a lead exists. The lead keeps Active, Dormant and Closed. **Four levels of demand status now**, which is logged as an accepted cost and the first place to look if a demand screen will not read | `upgrade-to-pros/VOCABULARY.md`, `upgrade-to-pros/DECISIONS.md` | C | **CD.** A lead cannot be in a qualifying state. Four status grains on one screen is the live risk |
| 2026-09-14 | **Project merging recorded as a need rather than a feature.** N-004, demand arriving later cannot join a sale in progress, and M-007, merge two projects, appetite deliberately unknown because the shaping is the work. **The finding: merging is the cross-time case, not the multi-trade case**, since a lead already holds several interests, and **a merge is cheap before a contract and expensive after**, so the real question is how late one is allowed. A cheaper alternative is named: qualify a second inquiry into the existing lead | `upgrade-to-pros/IDEAS-CATALOGUE.md`, `upgrade-to-pros/DECISIONS.md`, `IDEAS-INBOX.md` | C | **CD.** Do not draw a merge affordance. The alternative may remove the need entirely |
| 2026-09-14 | **A merge is prohibited once a project has a won proposal.** Andrew's ruling, and it makes project merging shapeable: every expensive case, a contract, an invoice, jobs, a payment plan, sits behind a won proposal and is therefore out of scope by definition. **M-007 appetite moves from unknown to a week.** The cheaper alternative is promoted to its own record, M-008, qualifying a new inquiry into an existing lead, **parked with a revive condition** because Andrew endorsed it for the future rather than now. It competes with M-007 rather than complementing it | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/IDEAS-CATALOGUE.md`, `IDEAS-INBOX.md` | C | **CD.** Do not draw a merge affordance on a project with a won proposal |
| 2026-09-14 | **Worked example restated a third time, onto the inquiry chain.** The Thompsons now have **two inquiries**, Robert's roof on 21 July and Sara's siding on 22 July, **resolving into one lead on 12 August** which creates the project. Windows was suggested internally and has no inquiry behind it. Every estimate, date, quoted word and outcome is unchanged across all three restatements. **The finding: two inquiries becoming one lead means qualification is already an act of routing demand**, so M-008 is not a new mechanism, it is the same act applied later in time | `upgrade-to-pros/WORKED-EXAMPLE.md` | C | **CD.** Re-read the demand section. Two inquiries, one lead, three interests, and one interest with no inquiry behind it |
| 2026-09-14 | **S14 taken up, and a second mock data set written.** `WORKED-EXAMPLE-S14.md`: Brightline Heating and Air, the Brenners as a first-time customer, one trade, money off-system. Built from the scenario's narrative and notes only, **after Andrew corrected a read that had used the generated Entities column**. **Four inconsistencies in the source flagged**, the sharpest being that beat 15 files a heat-pump rebate on an option that is not a heat pump | `upgrade-to-pros/WORKED-EXAMPLE-S14.md`, `upgrade-to-pros/OVERVIEW.md` | C | **CD.** Two data sets now. The Thompsons remain S15; the Brenners are S14 and are the empty-customer case |
| 2026-09-14 | **Memo 7 built: S14 end to end, fourteen sections, ten drawn and four placeholdered.** All twenty-odd beats, because the beats and the screens do not divide evenly. To-decide is whether the project is visible at all, since it now exists from two minutes into a phone call. **The hand-off is drawn as one screen to show that it should not be**, and section L carries an admitted Always violation: one line of explanatory copy on the close screen, because a crew lead holding a cheque will not read a memo | `upgrade-to-pros/memo-7-s14-end-to-end.html` | C | **CD.** Read the settled and to-decide cards first. Sections C, G and H are the three places the to-decide actually bites |
| 2026-09-14 | **Findings routed.** Deviations register gains memo 7: three faults in the scenario, five surfaces the script assumes without describing, and the admitted rule break. Object register gains **advisor availability with travel time**, **readiness gating between an order and a visit**, and **a follow-up after the work is finished**. **And a defect in our own status vocabulary**: the project set contains Paid with no path that skips it, so S14 can never reach a final state | `upgrade-to-pros/DEVIATIONS.md`, `upgrade-to-pros/OBJECT-REGISTER.md`, `upgrade-to-pros/VOCABULARY.md` | C | **CD.** Readiness gating is the cheapest thing in the register: everything the card shows already exists as a record, so the gap is the link rather than the data |
| 2026-09-14 | **Five component records, iteration 3.** TriageCard, MatchDismissalRow, ReadinessCard, DerivationReceipt and OffSystemMoneyCard, each with the seventh field. **The new field earned itself immediately**: ReadinessCard appears in two states across two sections of one memo and without a declared absorption it would have become two components | `upgrade-to-pros/components/ITERATION-3.md` | C | **CD.** Nineteen component records now. DerivationReceipt answers the receipt-for-a-derived-status need that has been open since the Thompson work |
| 2026-09-14 | **Claude Design context rewritten, version 3.0.** Version 2.0 was wrong in three places within a week, so the new one opens with a what-changed-since-you-were-last-briefed table and puts the cite-never-restate rule at the top. Also new: what is open and where to look, the seventh component field, the two data sets, and the design-system warnings about the token mirror and the Sol green | `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md`, `upgrade-to-pros/OVERVIEW.md` | C | **CD. Discard any stored copy of version 2.0.** The reading order has changed and the team's positions must be cited by row, not paraphrased |
| 2026-09-14 | **Build brief written for the S14 canvas.** Fourteen frames left to right: two new ones before memo section A, covering the search and the capture on the live call, ten from the memo, two placeholders, and **the proposal screens excluded this round** with a labelled gap on the canvas so the sale does not look like magic. The brief names the three questions the canvas is for and the one knowing rule break to preserve | `upgrade-to-pros/CD-PROMPT-s14-canvas.md` | C | **CD.** This is a build brief rather than context, so it says what to draw. Everything it does not say is governed by the standards |
| 2026-09-14 | **Deviation recorded before the canvas exists.** Memo 7 section A implies the system matched the customer in the background; the canvas makes it a search Dana runs. **The memo is the thing that should change**, not the canvas | `upgrade-to-pros/DEVIATIONS.md` | C | **CD.** Draw the search, not the background match, and the memo gets corrected afterwards |
| 2026-09-14 | **CD's S14 canvas bundle processed.** Fourteen frames carried into `prototypes/s14-canvas/`, stylesheet repointed to our token mirror and all 26 tokens it uses verified present. **Three new component records** from the canvas, CandidateMatchList, RoutingQuestion and CaptureFieldSet, plus a **TriageCard amendment**: the system-age question was not a new component, it widened an existing one, which is the seventh field stopping a component rather than describing one. **Two object-register rows**: a near-match search before a record is created, and a retained reason for a dismissal. Three deviation rows | `upgrade-to-pros/components/ITERATION-3.md`, `upgrade-to-pros/OBJECT-REGISTER.md`, `upgrade-to-pros/DEVIATIONS.md`, `prototypes/s14-canvas/` | C, from CD's write-back | **CD.** Twenty two component records now. The canvas supersedes memo 7 as the current drawing of S14 |
| 2026-09-14 | **Andrew's twenty five review comments recorded and routed**, verbatim, across the S14 canvas and the S15 nine-step file. **Four need his ruling**, and two of them are about the model rather than the drawing: his lead-tapped comment asks for a call to action that **creates a project, which no longer exists to create** under T-2, and his customer-record section proposal **nests jobs inside projects**, which reverses the 13 Sep no-indenting decision and collides with the canvas finding | `upgrade-to-pros/CD-REVIEW-01-responses.md` | C | **CD.** Do not start round two until rulings 1 and 4 land, because both change what a screen shows rather than how it looks |
| 2026-09-14 | **CD answered memo 7's to-decide, and the answer is about the model.** The container cannot be mentioned on any of the three frames where it bites, for three different reasons, none of them layout. Memo 7 said that if this is the answer the model is wrong rather than the layout. **It is the answer.** Also recorded: **the four-status test did not run**, because S14 has one interest so the interest and the lead are the same thing from every angle. The Thompson case is where four grains collide and it is untested | `upgrade-to-pros/CD-REVIEW-01-responses.md`, `upgrade-to-pros/OBJECT-REGISTER.md` | C, from CD | **CD and any surface designing demand.** The container has no early surface. Whether that means the container is wrong or correctly invisible is not settled by the canvas |
| 2026-09-14 | **Annotation rule corrected and completed.** New Always: annotation belongs to a memo, not to a canvas, because a canvas is judged as a design and annotation competes with the reviewer's own comments. **The old rule was incomplete rather than broken**, so CD carrying memo 7's markers onto the canvas was correct by the letter of it. Also recorded: **a collision CD named before it bit**, between the cards-in-pairs rule and a frame that genuinely has one thing on it | `standards/design-memo-format.md`, `standards/INDEX.md` | C | **CD.** No annotation on canvas frames. Put the argument in the frame label or the memo |
| 2026-09-14 | **Memo 7's own introduction was wrong about itself**, saying ten sections drawn and four placeholdered. It is eleven and three. Found by CD while building the frame table. Corrected in place | `upgrade-to-pros/memo-7-s14-end-to-end.html` | C | None |
| 2026-09-14 | **Three tooling quirks recorded.** Deletion in a connected folder is blocked until permission is asked for and the error does not say so. A zero-byte git index lock with a clean object database is an interrupted command rather than damage. And a canvas carried from the design surface needs its design-system link repointed at the moment of carrying | `SOURCES.md`, `upgrade-to-pros/DEVIATIONS.md` | C | **Every surface.** The first one cost two stray files that then could not be removed either |
| 2026-09-14 | **Two rulings, and one of them answers memo 7's to-decide.** **Start selling** is the act that moves interests from new to pursuing; it creates nothing, because the project already exists. The bundling question moves inside the lead, asking which interests rather than which other leads. And **the container becomes visible at the hand-off and not before**: the customer record lists leads only under selling, then groups by project with jobs nested once work exists. **Two independent routes reached that**, CD failing to name the container on three frames and Andrew wanting projects to group jobs. Supersedes the 13 Sep no-indenting decision, **which was right when made and stopped being load-bearing when three leads became one lead with three interests** | `upgrade-to-pros/DECISIONS.md` | C | **CD.** The container has a name on exactly one screen now, and it is the customer record after the hand-off |
| 2026-09-14 | **Memo 8 written: S14 round two, six screens rather than fourteen.** The first ninety seconds collapse into one capture screen with live matching and trade-typed questions, then a prompt that leads into booking. The customer record is drawn at two moments either side of the hand-off, which is where the container ruling is tested. The assessment becomes a card with a sheet, and the readiness screen takes two composition fixes. **Frames ten to fourteen and the booking screen stand as drawn**, because Andrew did not comment on them | `upgrade-to-pros/memo-8-s14-round-two.html`, `upgrade-to-pros/memo-7-s14-end-to-end.html`, `upgrade-to-pros/OVERVIEW.md` | C | **CD.** Redraw only the six. Memo 7 now carries a forward pointer saying which of its sections survive |
| 2026-09-14 | **Two admissions in memo 8, both flagged in it rather than hidden.** The customer record at four minutes old **does not advertise its empty capabilities**, which departs from the absence rule on the grounds that a four-minute-old customer would otherwise show three panels describing a product they have not bought. And the corrected readiness screen puts a lone full-width card above the groups, **which is the pairs-rule collision CD named before it bit** | `upgrade-to-pros/memo-8-s14-round-two.html` | C | **CD.** The lone-card question wants an answer in the composition standard rather than per screen |
| 2026-09-14 | **Memo 8 rewritten after three corrections from Andrew.** **Scope:** C had narrowed it to six screens and described that back as agreed when Andrew had only answered the rulings question. It now carries every screen, the nine that changed drawn and the five unchanged carried with what stands, **so CD has one input rather than a diff**. **Screen A becomes three**: blank, the trade resolving, the trade-typed form being worked, because the transformation is the argument and drawing only the end state hides it | `upgrade-to-pros/memo-8-s14-round-two.html` | C | **CD.** Rebuild from memo 8 alone. The carried section says what to take from the canvas unchanged |
| 2026-09-14 | **The template question is answered, and it answers three at once.** One or more trades are selected at the inquiry, which drives the inquiry questions, the site assessment templates and the hand-off. **And the inquiry and assessment may be one continued capture**, so the advisor is not re-asking what the CSR took down. **New to-decide in its place:** do the trades chosen at the inquiry become the interests, given that interests are defined as living inside a lead that does not exist yet | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/memo-8-s14-round-two.html` | C | **CD.** Trade choice is the first act on the inquiry screen, not a field among others |
| 2026-09-14 | **Correction to C, logged as a decision.** C told Andrew that a page showing a lead was not the project page. **It is both:** a qualified inquiry is a lead, a lead has a project from that moment, and during selling the two are coextensive. The visibility ruling is about when the container gets a name of its own, not about whether it is associated | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/memo-8-s14-round-two.html` | C | **CD.** Do not tell anybody a selling page is not the project page |
| 2026-09-14 | **Memo 8 revised on six comments.** Who-is-calling becomes a form on the capture screens. Potential matches is a **nested block inside the form** rather than a card beside it, with the dismissal reason on a tooltip. **Routing is off the inquiry screen entirely**, so the destination is either derived or a later act and neither is decided, which is now an open flow question. On the customer record: a Closed segment on Leads, the installed equipment card removed, Coming up nested inside the project card, and Activity moved up so Record is history alone | `upgrade-to-pros/memo-8-s14-round-two.html`, `upgrade-to-pros/components/ITERATION-3.md` | C | **CD.** Four component records amended. A nested block inside a card has now appeared twice and should be named before it appears a third time |
| 2026-09-14 | **Round two build brief written for CD.** Fourteen frames again: eight drawn new, five carried from round one, one redrawn with two moves. **Memo 8 is the specification and the brief says so**, so CD should not need memo 7, the old canvas or the comment file. Carries the three changes since CD last worked: no annotation on a canvas, team positions cited rather than restated, and the seventh component field with four amendments from the review. **Names the two things memo 8 leaves open on purpose** so CD does not resolve them quietly: the lone card against the pairs rule, drawn both ways, and whether the capture screen is a form or a read-back | `upgrade-to-pros/CD-PROMPT-s14-round-two.md`, `upgrade-to-pros/OVERVIEW.md` | C | **CD.** This replaces the round one brief. Do not put routing back on the inquiry screen and do not reinstate the installed equipment card |
| 2026-09-14 | **CD pushed back on the round two brief and was right about six of seven things.** Two were C's errors. **The tail frame is the project page at the close, not a customer record**, and the brief told CD to delete it, which would have removed the only page on the strip saying a new system exists at that address, the exact loss memo 8 raises as its own open question. **And the strip ran backwards at the end**, with a 30 July frame placed after a 7 August one. Both corrected | `upgrade-to-pros/memo-8-s14-round-two.html`, `upgrade-to-pros/CD-PROMPT-s14-round-two.md` | C, from CD's review | **CD.** The tail stays, memo 8 G is an additional frame at position 13, and the strip is fifteen frames in time order |
| 2026-09-14 | **Three more CD catches accepted.** Frame 01's trade list needed **real checkboxes**, because with annotation banned nothing else says five rows are a choice. The qualify prompt is **a dialog over the dimmed inquiry**, which draws its own argument better than a centred card on an empty page did. And **the stage rail compact treatment goes in after all**: it came from Andrew's S15 review and is a component amendment, so the brief was wrong to exclude it | `upgrade-to-pros/memo-8-s14-round-two.html` | C, from CD | **CD.** All three are in memo 8 now |
| 2026-09-14 | **A silent reversal declared and ruled.** Andrew asked for the trade and its questions in the right column; memo 8 drew them on the left and never said so. **CD caught it and said Andrew should rule.** He did: questions right, caller form left, matches nested in the form. **This is the failure the standards exist to prevent**, an instruction quietly inverted with a defensible reason and no declaration | `upgrade-to-pros/memo-8-s14-round-two.html` | C | **CD.** Columns are swapped on the three capture frames |
| 2026-09-14 | **Canvas frames are numbered, memo sections are lettered, and they never share a symbol.** CD proposed memo-prefixed letters because memo 7 H and memo 8 H collide. Numbers for strip position removes the collision entirely and keeps reading order obvious. Each frame label cites its memo section as provenance | `upgrade-to-pros/CD-PROMPT-s14-round-two.md` | C | **CD and every future canvas.** Worth treating as the convention rather than a one-off |
| 2026-09-14 | **The lone-card collision is settled rather than drawn.** A group may contain one card and it keeps its heading, because a heading is a semantic grouping and a page whose structure changes shape by card count is harder to hold. **CD pointed out the two treatments ended five frames apart and could not be compared by eye**, which made drawing both waste | `standards/ui-composition.md`, `standards/INDEX.md` | C, from CD | **CD.** Both frames keep their headings |
| 2026-09-14 | **SubordinateList recorded as one component, adopting CD's reasoning.** A list inside a card owned by the card rather than a peer of it. Matches carry a judgement and an action, coming-up reports and does nothing, and **that is row content the container absorbs**. Twenty three records now. **The seventh field worked as intended**: the question was asked in the brief before the canvas was drawn, answered up front, and cost one record instead of two | `upgrade-to-pros/components/ITERATION-3.md` | C, from CD | **CD.** Record it once |
| 2026-09-14 | **CD pack created, because CD could not read five of the seven files the brief named** and was holding a superseded context document. One generated file carrying the team's positions, the state of play, every rule, the S14 mock data and all component records. **It is a render and it goes stale**, so it says so at the top and names the date to check against | `upgrade-to-pros/CD-PACK.md` | C | **CD.** Upload the pack instead of seven files. Regenerate whenever CD is briefed |
| 2026-09-15 | **Open items register created.** One index of twenty five unresolved things, pointing at where each lives rather than restating it. **Nothing answered "what is still open"**, which is the first question a cold session asks, and the answer was spread across the positions file, two memos, four registers, the glossary and the review responses. Also recorded: **communications has surfaced five times in two days**, which is usually a sign something should stop being a register row and become a decision | `upgrade-to-pros/OPEN-ITEMS.md`, `upgrade-to-pros/OVERVIEW.md` | C | **Every surface.** Read it before asking Andrew anything, because most of what you would ask is in it |
| 2026-09-15 | **Two conventions logged as decisions rather than left in a brief.** Canvas frames are numbered and memo sections lettered, never sharing a symbol. And a render a read-only surface needs is generated as one file rather than a reading list, which is what the CD pack is | `DECISIONS.md` | C | **CD.** Both were working practice in the round two brief and are now rules |
| 2026-09-15 | **Handoff prompt written for the next session**, which moves to S15 against DRAFT v4. **The stated point is the component library rather than the screens:** whether one set of components and page templates can carry a single-trade job with the money off and a three-trade job with the money on, without proliferating near-identical siblings. **That is the first real test of principle G-1** | `upgrade-to-pros/NEXT-SESSION-PROMPT.md` | C | **The next session, and CD after it.** Four things S14 could not test are all things S15 exercises |
| 2026-09-15 | **A new object: the Opportunity.** Cross-sell demand has a record of its own, being a trade we think a customer might buy that they have not mentioned, recommended on evidence and converting to an interest when a lead is created. **It hangs off the customer and the property, not off an inquiry**, so a customer with no live demand can carry opportunities. Seven decisions logged. **Opportunity is deliberately un-retired** with one narrow live meaning, the decoder now carries three readings and the rule for dating them, and the collision is recorded at the root. The mark is called **To discuss**, not Should pursue, because Pursue and Pursuing already collide once and the interest set's second value is Pursuing | `upgrade-to-pros/in-depth/the-opportunity.md`, `upgrade-to-pros/VOCABULARY.md`, `upgrade-to-pros/DECISIONS.md`, `GLOSSARY.md` | C | **Every surface.** A fifth demand status grain exists. **Anything dated before today that says Opportunity means one of the two retired things**, and the decoder says which |
| 2026-09-15 | **The finding behind it, which is larger than the object.** Three facts were sharing one record: the system thinks they might buy it, we have decided to raise it, and the customer has engaged on it. **Origin as an attribute was never going to separate them**, which is the same gap the object register has open from the other end | `upgrade-to-pros/in-depth/the-opportunity.md`, `upgrade-to-pros/OVERVIEW.md` | C | **CD.** Any screen showing a suggested trade has been showing three facts as one |
| 2026-09-15 | **The canonical example's demand story rewritten. S15 now has one inquiry, not two.** The roof is the only customer contact; siding and windows are opportunities recommended on the 1978 build year and on photographs from the heat pump job a year earlier, both marked worth discussing and both raised by Northgate on the intake call. **Every figure, date and outcome unchanged.** Sara's siding words survive as an answer rather than the reason for a call, and she gains words about the windows because nobody used to ask her. **The 2025 install gains site photographs**, which is the evidence the siding recommendation rests on | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/DEVIATIONS.md` | C | **CD, and anything already built for S15.** The two-inquiry qualification moment does not exist any more. Four new deviations from the team's script, three of them proposals worth sending |
| 2026-09-15 | **A claim broke and it was load-bearing.** Two inquiries resolving into one lead was the argument that qualifying a new inquiry into an existing lead is needed on day one and therefore not a new mechanism. **With one inquiry that instance is gone**, so it is unproven again and project merging loses its cheap alternative | `upgrade-to-pros/OPEN-ITEMS.md`, `upgrade-to-pros/in-depth/the-opportunity.md` | C | None yet, but it should be said out loud next time merging comes up |
| 2026-09-15 | **Twenty four review comments filed and routed.** Both sets verbatim, plus a routing table. **Half the queue is visual and cannot go in a memo**, so it is routed to a build brief instead. Nine are structural, three cannot be acted on as written, one was overtaken by a model change made the day before it was written, and one was already half done. **The open items count was wrong**, saying fifteen where the real figure is thirty nine | `upgrade-to-pros/CD-REVIEW-02-routing.md`, `upgrade-to-pros/CD-REVIEW-02-s14-canvas-comments.md`, `upgrade-to-pros/CD-REVIEW-02-s15-ten-steps-comments.md`, `upgrade-to-pros/OPEN-ITEMS.md` | C | **CD.** Do not act on the leads-under-project comment as written: a lead is an engagement and there is nothing to nest |
| 2026-09-15 | **Three rulings are blocking the next memo**, all from the new comment set: whether qualification is being softened or only reworded, whether a lead gets a typed name against the named-by-contents decision, and whether a live call transcript is a real capability | `upgrade-to-pros/OPEN-ITEMS.md`, `upgrade-to-pros/CD-REVIEW-02-routing.md` | C | **Andrew.** Nothing should be drawn for the qualification moment until the first is answered |
| 2026-09-15 | **Two new standards, both from corrections made today.** **Walk the data first:** before drawing, walk the scenario's records in date order and ask of each whether it has somewhere to live at the moment it appears, and report a record with nowhere to live as a model defect rather than giving it a plausible parent. **Working a review queue:** sort into structural, visual and unactionable before answering any of it, check every comment against the decision log first, and say when one is overtaken rather than acting on it. **A memo cannot carry a visual comment and a brief cannot carry a structural one** | `standards/walk-the-data-first.md`, `standards/review-queue.md`, `standards/INDEX.md` | C | **Every surface.** Nine standards now. The index carries both in full |
| 2026-09-15 | **Corrections to the four rows above, found by a verification pass in the same session. Appended rather than edited, because this file is append only.** The standards count is **ten**, not nine. The review queue is **forty nine** comments, not thirty nine: the fifteen being corrected was only the S15 subset of the first round, and was itself sixteen rows. And the opportunity document said four rulings where the decision log gained seven | `CHANGELOG.md` is the record; the wrong figures were fixed in `upgrade-to-pros/OPEN-ITEMS.md`, `upgrade-to-pros/OVERVIEW.md`, `standards/review-queue.md` and `upgrade-to-pros/in-depth/the-opportunity.md` | C | **Nobody, if the rows above are read with this one.** The lesson is in the review-queue standard: check what the old number was counting before replacing it |
| 2026-09-15 | **Six documents were still asserting the old demand story and have been corrected.** The worked example's contractor table, the project overview's canonical-example paragraph, the ideas catalogue's argument for qualifying into an existing lead, the v4 beats document's first beat, the project workspace template record, and the demand-grain row in the open items. **The ideas catalogue one was the worst**: the withdrawn claim was the whole reason that move looked cheaper than project merging, and it was still stated in the present tense | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/OVERVIEW.md`, `upgrade-to-pros/IDEAS-CATALOGUE.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md`, `upgrade-to-pros/components/ITERATION-2.md`, `upgrade-to-pros/OPEN-ITEMS.md` | C | **CD.** The project workspace template's thinnest state has changed shape: under one lead the lineage diagram will not render, so that state now has three things on it and not four |
| 2026-09-15 | **Two long-standing errors fixed while checking the above.** The worked example's sequence table had **eight wrong weekdays**, unchanged since 11 September, and the project overview claimed a permit that the cash telling explicitly does not have. **Note the two closing dates now read as Saturdays**, which is true to the dates and odd for a narrative | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/OVERVIEW.md` | C | **Andrew.** The final bill lands on Saturday 26 September and the final payment on Saturday 3 October. Worth moving if any screen shows a weekday |
| 2026-09-15 | **The glossary miscounted its own vocabularies**, saying eighteen against nineteen index rows since the day it was written. Corrected. Also logged: the index's Always rule about adding a vocabulary to the glossary did not carry the glossary's own exception for the nouns of the business and the domain status sets, so a surface reading the index alone would have put the new status set in the wrong file | `GLOSSARY.md`, `standards/INDEX.md` | C | None |
| 2026-09-15 | **The Claude Design context document amended to 3.1.** Version 3.0 was wrong about demand from the moment the opportunity existed and said nothing about it. Three new rows in the what-changed table, the demand section rewritten to carry the object and the reason it exists, five status levels not four with the mitigation stated, the component record count corrected from nineteen to twenty three, and the comment routing file added to the reading list | `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md` | C | **CD. Re-read it.** A stored copy dated 14 September does not know the opportunity exists. **The CD pack is a render and is now stale too**, and should be regenerated before CD is next briefed rather than uploaded as it stands |
| 2026-09-15 | **The handoff prompt marked as used and as partly out of date**, rather than left to be pasted again. Demand has five grains not four, and the multi-trade inquiry is no longer taken twice on day one | `upgrade-to-pros/NEXT-SESSION-PROMPT.md` | C | **Andrew.** Write a fresh prompt rather than reusing this one |
| 2026-09-15 | **Qualification has three routes, settled.** Booking an appointment, assigning to sales, or a discrete mark, **and the discrete mark is retained alongside the other two rather than replaced by them**. "Next steps" is the label for a menu of the three and never for the act. **Two of the three qualify as a side effect**, so the normal case is that nobody presses the qualifying control, which is the fourth time this container's trigger has turned out to be invisible in practice. **Completeness logic prompts but does not qualify**: completeness is about fields and qualification is about whether the demand is worth selling to, and they come apart in both directions | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md`, `upgrade-to-pros/CD-REVIEW-02-routing.md` | C, from Andrew's ruling | **CD.** The qualification comment is closed and the answer resolves C's objection rather than overriding it |
| 2026-09-15 | **Auto-qualifying the Home App is worked out and not taken.** Andrew's concession, recorded with its consequences rather than logged as a decision. **It would put the lead and the project three weeks earlier with nobody present, turn the screen we were about to draw from an inquiry screen into a lead screen, and cleanly separate Start selling from qualification, which is exactly what S15 was chosen to test.** But every app contact making a lead means **routing later contacts into an existing lead becomes mandatory rather than cheap**, which reverses today's earlier finding. **And it exposes a tension between the two rulings of the same day:** if qualification tests intent then a phone call qualifies too and nothing is ever unqualified; if it tests structured data then it is completeness logic under another name | `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md` beat 3a, `upgrade-to-pros/OPEN-ITEMS.md` | C | **Andrew, and it is now the biggest open thing.** Three new open items, two of them blocking the memo. Nothing should be drawn for the capture screen until it is answered |
| 2026-09-15 | **The two closing dates moved off the weekend.** Correcting the eight wrong weekdays put the final bill and the final payment on Saturdays, so the close-out is now Monday 28 September and Monday 5 October. Everything before the close-out keeps its date and **the arithmetic is untouched** | `upgrade-to-pros/WORKED-EXAMPLE.md` | C | **CD, and anything already built.** Two dates changed in the money table, the sequence and the history |
| 2026-09-15 | **The CD pack is marked stale at the top and deliberately not regenerated.** Four specific things in it are now false, all named in the banner. **Regenerating now would produce a pack that is stale again before CD reads it**, because three rulings are open and the memo is not drawn. Regenerate at the moment CD is briefed | `upgrade-to-pros/CD-PACK.md` | C | **Andrew.** Do not upload the pack. Point CD at the repository and at version 3.1 of the context document until the rulings land |
| 2026-09-15 | **Qualification is automatic on expressed intent, and the lead is one per customer and property.** Two rulings that depend on each other. A contact naming a trade and a property is qualified without anybody marking it, so **the Home App always qualifies** and a phone call qualifies when somebody writes both down. **And a new contact joins the open lead rather than creating a second one**, which is what makes the first ruling safe: without it every app contact would make a lead and every lead a project. **The discrete mark survives as the exception path** for a contact naming neither. **The mechanism, that intent means a trade and a property, is C's reading rather than Andrew's ruling and wants confirming** | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md`, `upgrade-to-pros/VOCABULARY.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md` | C, from Andrew's ruling | **Every surface.** The capture screen is now a lead screen for most contacts, not an inquiry screen. **An unqualified queue still exists** but only for contacts naming neither a trade nor a property |
| 2026-09-15 | **The canonical example's dates moved again, and this time the rule held while the date changed.** The lead and the project are born **21 July**, when the app contact qualifies itself, with nobody at Northgate present. **12 August is now Start selling and creates nothing.** T-2 is untouched. **So the lead and the project are alive for twenty two days** holding three interests, no proposal and no money, which is a state no mock has drawn and the thinnest form the project template has to carry. **Roofing becomes an interest on 21 July with the lead; siding and windows convert on 22 July** on the intake call | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/OVERVIEW.md` | C | **CD, and every S15 artefact.** The three-week gap between qualification and Start selling is the thing S15 was chosen to test and it now exists in the data |
| 2026-09-15 | **Project merging may be dead, and one lead may have several projects.** Both fall out of the join rule. Merging existed because later demand made a second project that could not join the first; now it joins the open lead. **The hard case is relocated rather than removed**: demand arriving on a lead whose project holds a signed contract, answered by a sibling project rather than a merge. **Every previous document assumed one lead and one project** | `upgrade-to-pros/IDEAS-CATALOGUE.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md`, `upgrade-to-pros/OPEN-ITEMS.md` | C | **Every surface.** One lead to several projects is new and nothing had stated it |
| 2026-09-15 | **No naming moment: the container is renamable in place instead.** It arrives named by its contents and anybody can retype the name anywhere it appears, which gives Andrew the custom name he asked for without adding a naming step and without reversing the named-by-contents decision. **It created a question it did not have before:** the example now has the name grow twice and then shrink when the roof is deferred, and a shrinking name reads as loss rather than decision | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md` | C, from Andrew's ruling | **CD.** Every screen showing the container's name, which is most of them |
| 2026-09-15 | **Live call transcription recorded as a gap rather than drawn as a fact.** Criticality degrading, intent fill and probe, hold watch. **Watch rather than loose, because if transcription exists the capture screen is a different design.** The first thing to establish is whether calls are recorded at all, which is a platform fact and not a design one | `upgrade-to-pros/OBJECT-REGISTER.md` | C | **CD.** Draw it labelled as a proposal. Nothing may imply the transcript is real |
| 2026-09-15 | **Six open items closed and six opened.** Closed: whether qualification was being softened, whether the app auto-qualifies, intent against completeness, the typed name, the transcript, and routing demand into an existing lead. **Opened, and the first is load-bearing: what closes a lead?** If every contact joins the open one, the join rule has no end condition. Also whether the container's name may shrink, whether qualification is a gate or a reversal, and confirmation of the qualification test itself | `upgrade-to-pros/OPEN-ITEMS.md`, `upgrade-to-pros/CD-REVIEW-02-routing.md` | C | **Andrew.** Two of the three memo blockers are cleared. What closes a lead is the new one and it was not on anybody's list this morning |
| 2026-09-15 | **A second verification pass found eleven more things the qualification ruling broke, and one of them was conceptual rather than textual.** **The Opportunity's stated justification dissolved:** it was created that morning because a record had nowhere to live for twenty two days, and automatic qualification gave it somewhere to live by lunchtime. **The object stands on its other argument**, that three facts were sharing one record, which the ruling did not touch, and on the case of a customer carrying recommendations with no demand at all. **Recorded as an amendment at the top of the document rather than a rewrite**, because the homelessness was true when it was written | `upgrade-to-pros/in-depth/the-opportunity.md`, `upgrade-to-pros/DEVIATIONS.md`, `standards/walk-the-data-first.md` | C | **Every surface.** If the opportunity is ever argued for, argue it on the collapsed record and not on homelessness. **The deviation going to the team needs the second reason too** |
| 2026-09-15 | **A decision of 14 September was contradicted and not superseded.** It said the lead and the project are coextensive and one to one until a second lead or a job arrives. The join rule means a second lead never arrives, so its own escape clause could never fire. **Two live Working rows one day apart in the same table.** Now superseded by name, on cardinality only; its other claim, that a page showing a lead during selling is also the project page, still stands | `upgrade-to-pros/DECISIONS.md` | C | **CD.** The one-to-one holds at creation and not after |
| 2026-09-15 | **We may now differ from the team on a second thing.** T-2 reads "one lead, one project"; ours says one lead may have several projects over time. **Not recorded as a conflict**, because their wording is scoped to the moment the lead is created and is silent on later, **but close enough to ask rather than assume.** Logged as Raised | `upgrade-to-pros/TEAM-POSITIONS.md`, `upgrade-to-pros/DECISIONS.md` | C | **Andrew, and Joel eventually.** Second divergence after where a service agreement sits |
| 2026-09-15 | **S14 corrected for automatic qualification, and the pair got a better test out of it.** The Brenner call names a trade and an address in its first sentence, so it qualifies at **7:50am automatically**, not at 7:52 by a named CSR. **What Dana does at 7:52 is the triage**, which was always a separate judgement and is now visibly separate. **The contrast the pair now draws:** qualification and pursuit are two minutes apart in S14 and twenty two days apart in S15. Same two acts, same two objects, three orders of magnitude, one component set. **That is a better test than the interest count** | `upgrade-to-pros/WORKED-EXAMPLE-S14.md`, `upgrade-to-pros/OPEN-ITEMS.md` | C | **CD.** The S14 canvas shows a deliberate qualification act at 7:52 and it is now wrong. **Frames 03 and 04 are affected** |
| 2026-09-15 | **Seven closed rows deleted from the open items, per that file's own rule.** Each had been left in place with a note saying it was listed so nobody would reopen it, and **six were sitting in a section headed "Blocking the next round of design", answering the column "Why it blocks" with the word "Closed".** Closures are in the decision log, dated. The rule is now quoted in the file's header with what happened | `upgrade-to-pros/OPEN-ITEMS.md` | C | None. **The urge to leave a tick behind is what that rule exists to resist** |
| 2026-09-15 | **New standard: after a ruling.** Separate the principle from the mechanism and attribute each. Sweep for what the ruling breaks and what it creates, in the same session. Check that a superseded decision was superseded by name. **Check whether the ruling dissolved the problem that prompted it**, and if so find the other reason or withdraw the thing. **Re-read the other scenario, because nobody is looking at it.** Eleven standards now | `standards/after-a-ruling.md`, `standards/INDEX.md` | C | **Every surface.** All five rules come from things that went wrong today and were caught by verification rather than by the person doing the work |
| 2026-09-15 | **C was wrong and Andrew corrected it: one lead, one project, and the project contains multiple jobs.** C had inferred several projects over time from the join rule. **Later demand that sells lands in the existing project as another proposal and another job**, which is neither a merge nor a change order. **Three things withdrawn**: the several-projects row, the claim of a second divergence from the team's position, and C's supersession of the coextensive decision of 14 September, which is restored intact. **Project merging is now properly dead rather than relocated** | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/TEAM-POSITIONS.md`, `upgrade-to-pros/VOCABULARY.md`, `upgrade-to-pros/IDEAS-CATALOGUE.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md`, `upgrade-to-pros/OVERVIEW.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md`, `upgrade-to-pros/OPEN-ITEMS.md` | C, from Andrew's correction | **Every surface.** We agree with T-2 completely and there was never a second disagreement. **M-007, project merging, is dead and M-008 is the rule** |
| 2026-09-15 | **Two real defects found while working out where the wrong inference led, and both survive the right answer.** **The project can never reach a final state:** its set ends at Paid and nothing skips or reopens, so a project gaining a job in year three cannot read Complete. **And the lead's derived status has a hole:** a roof sold and in delivery, nothing else being sold and nothing deferred, is none of Active, Dormant or Closed. **Both are ours, not theirs**, and both resolve if a lead closes | `upgrade-to-pros/VOCABULARY.md`, `upgrade-to-pros/OPEN-ITEMS.md` | C | **Every surface. What closes a lead is now the most load-bearing question open**, because it is the join rule's end condition and the only thing that lets a project ever finish |
| 2026-09-15 | **The after-a-ruling standard paid for itself within an hour of being written.** The several-projects inference was recorded as C's reading, flagged in the reply, and logged as a possible divergence rather than as agreement. **Andrew corrected it in one line and the cost was one line.** Had it gone in as settled it would have been the premise of a screen. Also recorded: **the wrong path was still worth walking**, because the two defects it found are properties of the model rather than of the inference | `standards/after-a-ruling.md` | C | None. **The rule to keep is the attribution**, not the sweep |
| 2026-09-15 | **The demand side rebuilt, and the grain count went down for the first time.** **The Opportunity is now the durable demand object**: one record per customer, per property, per trade, with **origin as an attribute**, so a trade the customer asked for and a trade the system recommended are the same record. **It outlives every lead and every project.** Statuses Open, On a lead, Won and Dismissed, **Won the only terminal one**, and a declined trade **returns to Open** with a revive date and its history. **And the Interest is retired**: an opportunity is on a lead or it is not, and a proposal option prices it directly. **Four demand grains instead of five, and they are now different jobs rather than different gradings** | `upgrade-to-pros/in-depth/the-opportunity.md` rewritten, `upgrade-to-pros/VOCABULARY.md`, `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/OVERVIEW.md`, `upgrade-to-pros/CLAUDE-DESIGN-CONTEXT.md` | C, from Andrew's ruling | **Every surface. Stop using the word Interest.** The behaviour the team's position describes is untouched; only the object is gone, and that is a vocabulary difference to raise with them rather than a disagreement |
| 2026-09-15 | **A lead is a selling episode and closes at the hand-off.** Everything on it is either Won or back to Open, so it has nothing left to do. **In the canonical example the lead closes 24 August, not 5 October**, and the project delivers for six weeks with no lead behind it. **The join rule applies only while a lead is open**, so a contact during delivery starts a fresh lead and project, which is correct because it is a different sale | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/VOCABULARY.md`, `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md` | C, from Andrew's ruling | **CD.** Two states nothing has drawn: a lead and project alive for twenty two days holding one opportunity, and a project delivering with its lead closed |
| 2026-09-15 | **Six open questions closed and two blocking object-register rows folded into the model**, all by the same change. What closes a lead, the project never reaching a final state, the hole in the lead's derived status, where several trades live, whether an unmarked opportunity expires, and what a disqualified inquiry leaves behind. **Plus lead origin as a first-class attribute and a queue that surfaces a revive date**, both blocking since the Thompson work. **Neither was built**: both were answered by a modelling change made for another reason | `upgrade-to-pros/OPEN-ITEMS.md`, `upgrade-to-pros/OBJECT-REGISTER.md`, `upgrade-to-pros/TEAM-POSITIONS.md` | C | **Every surface.** An argument for keeping a register of gaps rather than working through it in order |
| 2026-09-15 | **The S15 demand story rewritten a fourth time, and the deviation to the team simplified with it.** One inquiry and **three opportunities**, roofing from the customer and two recommended on evidence. Roofing on the lead from 21 July; windows and siding Open until Start selling on 12 August; roofing back to Open on 18 August. **Every figure, date and outcome unchanged through all four rewrites.** **Two earlier reasons for the deviation are withdrawn** and it now rests on durability alone: a declined roof stays visible under the customer with its history, where their scenarios handle that in prose | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/WORKED-EXAMPLE-S14.md`, `upgrade-to-pros/DEVIATIONS.md`, `upgrade-to-pros/IDEAS-CATALOGUE.md` | C | **CD, and every S15 artefact.** S14 now reads one opportunity rather than one interest, and its qualification is automatic at 7:50 |
| 2026-09-15 | **A third verification pass found the beats document had become the main liability, and it is now concluded.** Eight places still treated the Interest as a live object, its one-line summary said both the lead and the project live until the close, and sections 4 and 6 compared two objects that no longer both exist. **Concluded with a banner naming which sections are live and which are history**, per the in-depth folder's own rule. Sections 3a, 3b and 3c stay live; 1, 3's route table, 4, 5 and 6 are marked history | `upgrade-to-pros/in-depth/lead-becomes-a-project-v4.md` | C | **Every surface. Read `in-depth/the-opportunity.md` for the live model of demand**, not the beats document |
| 2026-09-15 | **Eleven further contradictions cleared across nine files.** The worked example said two opportunities in one place and three in another, and had a declined trade still sitting on a closed lead. The project overview's canonical-example paragraph still said three interests. The vocabulary's decoder carried the pre-widening definition of an opportunity. **T-11, project merging, was still in the team's current positions table** and is now marked withdrawn, having always been Andrew's rather than theirs. M-007 is dropped and M-008 no longer reads Parked. **Two history files gained staleness banners**, the round one build brief and the 14 September review responses, because both assert model claims that are now wrong and neither said so | `upgrade-to-pros/WORKED-EXAMPLE.md`, `WORKED-EXAMPLE-S14.md`, `OVERVIEW.md`, `VOCABULARY.md`, `TEAM-POSITIONS.md`, `IDEAS-CATALOGUE.md`, `CLAUDE-DESIGN-CONTEXT.md`, `MODEL-SNAPSHOT.md`, `CD-PROMPT-s14-canvas.md`, `CD-REVIEW-01-responses.md` | C | **CD.** The round one brief and the first review round both carry wrong model claims and now say so at the top |
| 2026-09-15 | **The mark's name was used three ways in one file** and is settled as **worth raising** throughout. It was To discuss while it was a status; it is now an attribute on an Open opportunity, so the collision that produced the old name no longer applies | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/VOCABULARY.md` | C | **CD.** Anything drawing the mark says worth raising |
| 2026-09-15 | **O-36 deleted from the open items, answered.** What closes a lead is everything on it being resolved, which is the hand-off. **It answered two defects on its way past** and both are deleted with it. Thirty rows open, none marked closed, which is what that file is supposed to look like | `upgrade-to-pros/OPEN-ITEMS.md` | C | None |
| 2026-09-15 | **Memo 9 built: S15 end to end, eleven screens.** The run from the morning a form arrives to the morning the money clears, against the demand model as it stood at the end of the day. **Two screens nothing had drawn before**: a lead and project alive for twenty two days holding one opportunity, and a project delivering for six weeks with its lead already closed. **The opportunity is drawn for the first time**, on four screens plus a section of its own on the customer record. **One coloured region in the whole memo**, the change order, because the cash no-permit telling never blocks anything else. **Money is placeholdered on six of eleven screens** at Andrew's instruction | `upgrade-to-pros/memo-9-s15-end-to-end.html`, `upgrade-to-pros/OVERVIEW.md` | C | **CD.** This is the current S15 position. **The to-decide card is whether the opportunity survives being drawn**, and a failure there means the model is wrong rather than the layout |
| 2026-09-15 | **The first real evidence for principle G-1, and it is mixed.** Composing a scenario rather than drawing it is the condition G-1 set before anything could be justified on its grounds, and this is the first time it happened. **Nine components carried both scenarios unchanged**, and **PeerRow carried six kinds of object across eleven screens with only the status line differing**, which is what the anti-proliferation claim looks like if it is ever demonstrated. **Lineage died**: it will not render below two leads and S15 now has one, so a component built for the multi-trade case cannot draw the multi-trade case. **SubStatusRow failed in the opposite direction to the one its own seventh field predicted**: the warning was too few rows, the reality is a full card with nothing blocking. **One new component, CallTranscript**, and two templates amended | `upgrade-to-pros/components/ITERATION-4.md` | C | **Every surface.** A component can be correct, stay correct, and have the shape it draws stop existing. **The library standard has no word for that** and probably needs one |
| 2026-09-15 | **The money test has now been deferred twice and it is the clearest gap left.** MoneyCard and OffSystemMoneyCard are two components for one job, differing only in whether the product collects. **S14 exercises one and S15 the other, which is exactly the comparison that would settle it.** Memo 9 placeholdered money on six screens, so it was not run | `upgrade-to-pros/components/ITERATION-4.md` | C | **Andrew.** Recommend the next round is money, on both scenarios, and nothing else |
| 2026-09-15 | **Memo 9 sections A and B reworked on Andrew's review.** **Roofing is lifted into a full-width card of its own** headed On this lead, with the recommendations below it under **Other opportunities worth raising**, and the segmented control is gone. **The separator he asked for became the second card's heading**, which is cleaner, and it exercises the lone-card rule of 14 September deliberately for the first time. **Wording checked against the vocabulary**: his phrase was "to pursue", which reintroduces a stem removed this afternoon, so it reads "worth raising" to match the mark on the rows | `upgrade-to-pros/memo-9-s15-end-to-end.html`, `upgrade-to-pros/components/ITERATION-4.md` | C, from Andrew's review | **CD.** The filter is gone from that card, which costs nothing at three opportunities and a lot at twelve. Dismissed opportunities now have no home on that screen |
| 2026-09-15 | **The accordion rule lost its first argument, and the exception is declared rather than taken quietly.** Memo 9 section B uses three collapsible sections instead of a segmented control, roofing open. **The reason: a person capturing during a live call cannot control the order the answers arrive in**, a sheet would cover the transcript, and a segmented control hides two trades behind a click. **Two conditions make it an exception rather than a weakening**: counts move onto the collapsed headers, and one section is open by default. **The cost is recorded**: what the customer said about the other two trades is now behind closed headers | `standards/ui-composition.md`, `standards/INDEX.md`, `upgrade-to-pros/memo-9-s15-end-to-end.html`, `upgrade-to-pros/components/ITERATION-4.md` | C, from Andrew's instruction | **CD.** New component, CollapsibleSection, and its two conditions are part of the component rather than advice |
| 2026-09-15 | **Section D corrected to match the reworked section A, which would otherwise have broken the point of the section.** D exists to show that twenty two days change almost nothing, and it was still drawn with the single card and filter that A had before the review. **Two screens meant to be identical were structurally different**, found by checking the memo's own claim rather than by reading it | `upgrade-to-pros/memo-9-s15-end-to-end.html` | C | None |
| 2026-09-15 | **SubordinateList is now unexercised in the current S15 material.** Its only use in memo 9 was the nested block in the call card, which the collapsible sections replaced. **It was named on 14 September after appearing twice in one memo and lost one of those uses within a day.** Still used on the S14 canvas, so not dormant, **but a component that loses a use that fast may have been named too early** | `upgrade-to-pros/components/ITERATION-4.md` | C | **CD.** Worth checking before it is relied on |
| 2026-09-15 | **Memo 9 reworked on the second review, and two of the changes are to the data rather than the drawing.** **The front of the story is compressed from twenty two days to six**: the inquiry moves to 4 August, the call to 5 August, Start selling to 10 August, and **everything from 13 August onward is untouched**, so the proposal, presentation, contract, work and money keep their dates. **And Northgate gains a CSR**, Renata Silva, who takes the intake call and hands the lead to Priya. **Section D is deleted** because the resting state it existed to show is no longer long enough to be a problem, so the memo is ten screens and the letters shift | `upgrade-to-pros/memo-9-s15-end-to-end.html`, `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/DEVIATIONS.md` | C, from Andrew's review | **CD, and every S15 artefact.** **Two costs, both recorded as deviations:** the gap was what made S15 test Start selling as a separate act, and Northgate now runs the same shape as Brightline, which removes one axis of difference between the pair |
| 2026-09-15 | **Seven further changes to memo 9.** Screen A's primary becomes **Engage customer** with three routes, which fixes the defect that nothing on A led to B. Screen D is a **modal**, not a sheet, **which the component records were already right about**: a row opens a sheet and a decision opens a dialog. Screen B gains a notes field per trade and loses the not-asked field, **which was the only demonstration of that treatment**. Screen G gets **a card per job** carrying where it is, what is next and what it is waiting on, **which answers the SubStatusRow failure from the other direction**, and Attributes becomes **Provenance**. The customer page uses one name. **A light grey pencil sits beside every customer, lead, project and job title**, which is the affordance the renamable-in-place ruling implied and nothing had drawn | `upgrade-to-pros/memo-9-s15-end-to-end.html` | C, from Andrew's review | **CD.** The per-job card scales to two and awkwardly to five, which is worth knowing before a scenario with more trades |
| 2026-09-15 | **S15B exists as a scenario of its own, on a Confluence page under their Scenarios parent.** Our version had drifted from S15 in about a dozen recorded ways and calling it "S15 with deviations" had stopped being honest. **Naming it turns a growing list of departures into a definition and leaves S15 exactly as the team wrote it.** Twenty nine beats, Step and What Happens filled and the other two columns deliberately blank, four differences named at the top, and a closing argument aimed at the two open items their own page already carries | A new Confluence page, `S15B · Multi-trade expansion, opportunity-led (cash)`, and `upgrade-to-pros/DECISIONS.md` | C | **Every surface, and Joel eventually.** Our worked example is S15B from now on, not S15 |
| 2026-09-15 | **Drift found, and it had been sitting two days.** **Their S15 was rewritten into the v4 model on 14 September** and our register said 4 September. **Two of our positions turn out to be theirs already:** their beat 2 mints the project at the lead, and their beat 4 has the system suggesting the windows, so the recommendation is in their script rather than being our invention. **They still run Interest as a live object**, which we retired today. **And they carry two open items our Opportunity answers, in their own words**, which is a stronger argument than anything we would have written. **Most of the scenario set moved on 14 and 15 September**, including S14 and S14B | `upgrade-to-pros/DRIFT-CHECK.md` | C | **Every surface. Anything built against S14 should be rechecked**, because S14 and S14B both moved today |
| 2026-09-15 | **The drift routine has the wrong trigger, and this is the second time today a check fired by accident.** It was not run because nothing prompted it; **writing a document that had to cite their page is what prompted it.** Recorded as a suggestion: attach the check to the act of citing a source rather than to a date | `upgrade-to-pros/DRIFT-CHECK.md` | C | **Andrew.** Worth a standard if it happens a third time |
| 2026-09-15 | **Canvas frames now share the memo's letters, reversing this morning's convention.** Frames read "Memo 9 · A" rather than being numbered. The old rule existed because memo 7 section H and memo 8 section H are different screens, **which is a two-memo problem**; with one memo and one canvas in lockstep there is nothing to collide with. **The memo name in the frame label is what stops it returning** | `upgrade-to-pros/DECISIONS.md`, `upgrade-to-pros/CD-PROMPT-s15-end-to-end.md` | C, from Andrew's instruction | **CD and every future canvas** |
| 2026-09-15 | **Memo 9 gains the money and a metadata strip on every section.** The money is drawn in **four states across five screens**: no tender chosen at the proposal, cash part paid at the hand-off and during the work, read-only at a job with a badge saying where it is held, and settled at the close. **Per-job margin is absent and nothing marks its absence**, which is the density rule and the money principle agreeing. **Every section now carries the S15B beats it covers and who is on the screen**, so the memo and the canvas share the same metadata | `upgrade-to-pros/memo-9-s15-end-to-end.html` | C, from Andrew's instruction | **CD.** The money comparison against S14 is the one that decides whether the two money cards should be one component, and it is no longer deferred |
| 2026-09-15 | **Build brief written for CD, with a four-item repo checklist in front of it.** Regenerate the pack, re-extract the Sol tokens, check S15 for drift, and decide whether CD gets the S15B page. **The token file is the one that bites**: the brief asks CD to match existing styles and what we hold is the Merlin mirror with no radius, spacing or font scale, flagged since 11 September. **The pack banner now says regenerate rather than wait** | `upgrade-to-pros/CD-PROMPT-s15-end-to-end.md`, `upgrade-to-pros/CD-PACK.md` | C | **Andrew.** The four items are yours to do before the prompt is run. **Do not upload the pack as it stands** |
| 2026-09-15 | **Two screens added at the front of memo 9, and it grew to twelve.** **A is the customer record at rest on 3 August** and **B is the same page at 7:12am on 4 August**, seconds after Robert's app contact lands. **Everything that changes between them changes by itself**, which is the clearest demonstration of automatic qualification in the run, and neither state had ever been drawn. **C is reached by tapping the lead on B.** Everything else shifted from A to J into C to L | `upgrade-to-pros/memo-9-s15-end-to-end.html`, `upgrade-to-pros/CD-PROMPT-s15-end-to-end.md` | C, from Andrew's instruction | **CD.** Twelve frames now, lettered A to L, and the brief's table is rewritten |
| 2026-09-15 | **The two recommendations now predate the enquiry by eleven months**, at Andrew's instruction. Made on 26 September 2025, two days after the heat pump project closed, off the install photographs and the property record, **and never mentioned to anybody until the intake call.** **This makes the cross-sell record exist independently of demand**, which is the state a customer record spends most of its life in and which neither model had to represent. **And it makes the staleness question concrete**: nothing ages a recommendation out, and eleven months is long enough for that to stop being theoretical | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/memo-9-s15-end-to-end.html`, and the S15B Confluence page | C, from Andrew's instruction | **Every surface.** The opportunity's expiry question, O-26, now has a number on it and a screen showing it |
| 2026-09-15 | **A contradiction on the old screen A, found while wiring the flow.** The card was headed "Other opportunities worth raising" while its own footer offered "Mark worth raising", so **the heading claimed the marking was done and the footer offered to do it.** Corrected: the heading is "Other opportunities at this property" and the marking happens on that screen, which is what Andrew's flow implies | `upgrade-to-pros/memo-9-s15-end-to-end.html` | C | None. **Found by drawing the transition rather than by reading the page** |
| 2026-09-15 | **S15B updated to version 2**: five differences rather than four, the recommendations moved into beat 1, a new beat 2 for the household at rest, and a closing note that nothing in either model ages a recommendation out. **Beats 4 onward are unchanged**, so every citation still resolves | The S15B Confluence page | C | **CD.** The memo and the brief both cite beats by number and both are aligned to version 2 |
| 2026-09-15 | **A verification pass on memo 9 and the brief found twenty eight errors and all are fixed.** **Six were factual**: the storm still dated July, a heat pump described as a year old at six months, the intake note attributed to Priya rather than Renata, the two recommendations described as arriving the same morning when they are eleven months old, a claim that the assessment changed the price when it changed the scope, and jobs given dates four days before those dates were agreed. **Six were wrong section letters**, all created by inserting two screens at the front. **Four were the memo arguing with itself**: a heading counting two badges above a body counting three, a nested block described as both gone and still competing, a modal called a sheet in its own beat line, and a primary action named as Start selling when the screen draws Engage customer. **And five drawn money cards were sitting under a group heading that said Not drawn this round** | `upgrade-to-pros/memo-9-s15-end-to-end.html`, `upgrade-to-pros/CD-PROMPT-s15-end-to-end.md` | C | **CD.** The brief also told CD to head a card "Other opportunities worth raising", which is the exact error the memo records as fixed. **Following the brief would have reintroduced it** |
| 2026-09-15 | **Three CSS rules were referenced and never defined**, so every money total rendered without its rule above it and without emphasis, and one colour token was undefined. **Found by verification, not by looking at the page** | `upgrade-to-pros/memo-9-s15-end-to-end.html` | C | None. Worth remembering that a mock kit copied between memos loses rules silently |
| 2026-09-15 | **A git lock file was left in the repository and the cause is now a rule.** `git status` and `git diff` refresh the index as a convenience, which writes `.git/index.lock`, and **the sandbox can create that file but cannot unlink it**, so an ordinary status check leaves a lock that blocks the next commit. **`git --no-optional-locks status` returns the identical answer and leaves nothing.** Removing the stray one needed folder delete permission, which is a separate prompt. **The quirks table already had a row on diagnosing the lock**, which is the wrong half of the problem: knowing how to clean up after a tool is worth less than knowing how not to trigger it | `standards/repository-hygiene.md`, `SOURCES.md` | C, found by Andrew | **Every surface with a shell.** Andrew found this, twice over: the lock, and the fact that the quirks table had been recording the symptom |
| 2026-09-15 | **S15 rewritten on the team's own page, at Andrew's explicit instruction.** Version 14. Inbound phone call rather than Home App, **two trades rather than three and both sold**, a $500 deposit with $72,600 on completion rather than three milestones, **no change order and no HOA step**, permits on both jobs, and both jobs running in the same week. Twenty one beats. **The Notes and Entities columns were cleared, so Joel's authored annotations are off the page** and recoverable only from history. **The five sections below the script are untouched and each now contradicts it**, so each carries a banner saying which parts of it are dead | The S15 Confluence page, `upgrade-to-pros/DECISIONS.md` | C, on Andrew's instruction | **Everyone who reads that page.** The survivor branch, the fail branch, the P0 to P9 table, what-it-produced and the carried-over reviewer comment are all now about a story that is not on the page |
| 2026-09-15 | **S15B superseded three hours after it was created, for the opposite reason it was created.** It existed so S15 could be left alone; the narrative then went onto S15 itself. **Marked superseded with a note on what it carried that is now nowhere**: the Opportunity as a durable per-trade record, and a recommendation predating demand by eleven months. **Both survive in the repository rather than in Confluence** | The S15B Confluence page | C | **Every surface.** Do not design from S15B |
| 2026-09-15 | **We are now a writer on a source we track for drift, and the register cannot handle it.** Drift is detected by comparing a page's last-modified date with the date we last read it. **Our own edit moves that date**, so the next check will see S15 modified on 15 September and cannot tell our writing from theirs. **For S15 only, compare against version 14 rather than a date** | `upgrade-to-pros/DRIFT-CHECK.md` | C | **Every surface that runs the drift routine** |
| 2026-09-15 | **And a safeguard was lost rather than inconvenienced.** The deviations register exists to record where a mock departs from the team's scenario, and **the S15 rows in it are now meaningless because S15 is our script**. The discipline was that a departure had to be written down and could then be sent to Joel or withdrawn. **Writing our narrative into their page removes the gap the safeguard measured** | `upgrade-to-pros/DRIFT-CHECK.md` | C | **Andrew.** Worth knowing before the same is done to another scenario |
| 2026-09-15 | **Three things about the new S15 recorded as decisions rather than left in the beats.** **Nothing in the scenario is blocked and nobody has to act**, which the deviations register already logged once as a mistake and which is now true of the whole story rather than one column. **A job carries working detail the contract does not**, because the coordinator refines each scope after the hand-off, which is C's reading and wants confirming. And **$500 on $73,100 means Northgate funds $72,600 on trust for six weeks**, which is deliberate and commercially unusual | `upgrade-to-pros/DECISIONS.md` | C | **CD.** The blocked state, the readiness gate and the attention treatments are now untested in both scenarios |
| 2026-09-15 | **Memo 10 built from the rewritten S15, fourteen screens against twenty one beats.** Its UX Specific column is the specification, so **six beats are deliberately not drawn** and listed as such: the proposal, the tender and contract, the hand-off, the lead closing, and the materials delivery. **The money had to be worked out across the whole script** because only beat 7 gives a figure and beat 11 says use judgement. It lands as **five readings of one card**: nothing owed, not yet due, due but uninvoiced, invoiced, paid. **Two format departures, both declared in the file**: the beat and its instruction sit in a rail beside the mock rather than the argument sitting underneath, and **there is no `[i]` annotation anywhere** because the request was mocks next to beats. Under each mock: what was taken out, what needs a home, and the question | `upgrade-to-pros/memo-10-s15-script-walk.html` | C, from Andrew's instruction | **CD.** The brief and memo 9 are both aligned to a story that no longer exists. Memo 9 has three trades, a change order and an off-system payment; this script has two trades, no change order and a card |
| 2026-09-15 | **"Due, no invoice" is a state the model does not have, and the new script produces it.** The balance falls due on completion at beat 19 and the invoice is raised at beat 20, so **for three days $72,600 is owed and nothing has asked for it.** It is the only amber in the whole run. **The reading five screens now rest on: the payment plan lives at the project and carries both milestones, and an invoice is raised per milestone when its trigger fires**, so beat 20 raises the second invoice rather than the first. If that is wrong, sections G, H, K, L and N are all wrong | `upgrade-to-pros/memo-10-s15-script-walk.html` | C | **Everyone.** This is the first of the memo's five closing questions and the most expensive |
| 2026-09-15 | **The collapsible-section record contradicts itself across two screens in the same memo.** On the intake call one section is open and adding a trade closes the previous one, which is what the component's record predicts. **On the site assessment both trades are open at once**, because somebody standing on a roof recording two trades needs both. The component's seventh field says what variation it absorbs and **it does not absorb this one** | `upgrade-to-pros/memo-10-s15-script-walk.html`, `upgrade-to-pros/components/ITERATION-4.md` | C | **CD.** Amend the record or make the assessment something other than an accordion |
| 2026-09-15 | **Three things the new script breaks that were settled treatments.** **Money is absent from the job screen entirely**, where memo 9 drew it read-only with a badge saying where it is held, and the two cannot both be right. **There is no Waiting on card anywhere**, because the permits are the only dependency and they are issued, so a card built to separate the controllable from the uncontrollable would read as good news and say nothing. And **the plan is badged Cash throughout while the deposit is a cheque and the balance a credit card**, which nothing in the model resolves | `upgrade-to-pros/memo-10-s15-script-walk.html` | C | **CD.** Two tender instruments on one plan is new and the money card has no state for it |
| 2026-09-15 | **Two gaps the memo found that are the model's rather than the layout's.** **The project has no task list of its own**, which section H makes visible by putting two job checklists side by side with nothing belonging to the container, and there are two permits in this script that would have nowhere to live if they were shared. And **the job screen links to "what changed" on a scope refined after the hand-off, which nothing records**, so the link is a promise the model cannot keep | `upgrade-to-pros/memo-10-s15-script-walk.html` | C | **Andrew.** Both are register rows rather than memo revisions |
| 2026-09-15 | **Memo 10 revised on Andrew's review, nine changes.** **Money becomes a section in its own right on every screen that has it**, headed simply Money rather than a natural-language heading, and **the card's own title comes off** because the group heading already says it, leaving the tender badge to qualify the card. **Money moves below Previous work on the customer record** rather than sitting beside the contact details, so the customer page reads contact, then history, then money. **Every Work group is now Jobs** | `upgrade-to-pros/memo-10-s15-script-walk.html` | C, from Andrew's review | **CD.** "Money" is now a section name rather than a description, which means the heading no longer tells a reader what changed since the last screen |
| 2026-09-15 | **The proposal card is gone and its figures moved inside the money card.** The contract total carries a nested breakdown of the two job prices, which is the same slot the script's deferred cost breakdown would have to occupy. **Two breakdowns of one total in one place is a collision worth seeing now** rather than when costs arrive. **The balance-due row is nested on the two screens where nothing is due yet**, so a condition no longer sits in a column of figures pretending to be one | `upgrade-to-pros/memo-10-s15-script-walk.html` | C, from Andrew's review | **CD.** The money card is now one component doing five jobs across five screens |
| 2026-09-15 | **The transcript is back, at Andrew's instruction, under Trades on the intake screen.** **The capability does not exist and the UX column does not ask for it**, so the gap stands and the memo says so under the mock rather than letting the badge reading Live imply otherwise. It appears from the screen where a trade is chosen, because before the customer match there is nothing to attach it to | `upgrade-to-pros/memo-10-s15-script-walk.html` | C, from Andrew's instruction | **CD.** Live transcription remains a recorded gap, drawn twice now across two memos |
| 2026-09-15 | **Job statuses become badges and a filter came off.** Complete takes a success tint and everything else is neutral. **This breaks the one-coloured-region rule on the close-out screens, where both jobs are complete and the colour distinguishes nothing**, and consistency was taken over density deliberately. The all/active/complete filter came off the mid-week screen: **with two jobs it was filtering a set a reader can see**, and the work it was pretending to do is now done by a line under the balance | `upgrade-to-pros/memo-10-s15-script-walk.html` | C, from Andrew's review | **CD and the density standard.** Two green badges plus an amber one on one screen is the collision, and it is declared in the memo rather than hidden |
| 2026-09-15 | **Create invoice moves off the page header and into the money card**, which puts the action beside the figure it acts on and **exposes that one card now carries the same fact four times**: a warning badge, a coloured total, a status line reading "Due, no invoice", and the button. Three are removable. **And what was left at the property came off the close screen**: it outlives the project, so it belongs to the property, and it was the only thing in the memo drawn at the wrong level | `upgrade-to-pros/memo-10-s15-script-walk.html` | C, from Andrew's review | **CD.** The close screen is now a single money card, which makes the ending of a $73,100 project unusually quiet |
| 2026-09-15 | **A new CD brief for memo 10, fourteen frames, and the morning's brief marked superseded.** Two briefs for the same scenario were live inside one day, **built from two memos whose letters mean different screens and whose stories are incompatible.** The memo-name prefix in the frame label, invented this morning, is what stops that being a silent collision, and **it was needed within hours of being written.** The new brief's central ask is the money card: **one card doing five jobs across six screens**, and whether that is one component with five states or more than one | `upgrade-to-pros/CD-PROMPT-s15-script-walk.md`, `upgrade-to-pros/CD-PROMPT-s15-end-to-end.md` | C | **CD.** Do not run both. The superseded one names S15B as the scenario and asks for four money states when there are five |
| 2026-09-15 | **The worked example and the memo now disagree, and nothing in the file could have prevented it.** The worked example describes three trades, the Home App contact, a change order and a three-milestone plan; **the memo has two trades, a phone call, permits on both jobs and two milestones.** Its rule covers inventing a value without adding it, and **the memo invented four**: the siding crew lead, the plan price, the siding material, and the office manager at the invoice. **But the real failure is that the story under the file moved, and the file has no rule for that.** A mock data file needs a version of the narrative it belongs to. **Until it is rewritten the brief tells CD to ignore it**, which is the file's whole purpose inverted | `upgrade-to-pros/WORKED-EXAMPLE.md`, `upgrade-to-pros/CD-PROMPT-s15-script-walk.md` | C | **Andrew, and every surface.** This is the largest outstanding item in the project and a prompt cannot fix it |
| 2026-09-15 | **The permit dates in memo 10 contradicted the beat they were drawn from, and are corrected to a single day.** The script merges applying and issuing into one beat; the mock had applied on 13 August and issued on 20 August, **which invented a week-long wait in a story whose own premise is that nothing is blocked.** Both now read 13 August, so the section date moves too. **Seven places.** Weekdays across the whole memo were re-verified and all are correct | `upgrade-to-pros/memo-10-s15-script-walk.html` | C | **CD.** Frames H, I and J all carried the wrong permit date |
| 2026-09-15 | **The token file has survived two rounds of being flagged, so the workaround is now written into the brief.** Verified today: 113 tokens, **colour and elevation only**, no radius, no spacing, no type scale. The brief tells CD to take those three from its own components **and to list what it chose**, so the re-extraction has something to check itself against. **A workaround repeated twice is a decision** | `upgrade-to-pros/CD-PROMPT-s15-script-walk.md`, `reference/merlin-sol-tokens.css` | C | **Andrew.** The re-extraction is still not done and is now two rounds overdue |
